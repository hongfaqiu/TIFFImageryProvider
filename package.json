{
  "name": "root",
  "private": true,
  "npmClient": "pnpm",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "pnpm run build:lib",
    "build:lib": "pnpm -r --filter \"./packages/**\" run build"
  },
  "devDependencies": {
    "lerna": "^6.1.0"
  }
}
